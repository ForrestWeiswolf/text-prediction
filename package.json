{
  "name": "text-prediction",
  "scripts": {
    "start": "concurrently \"(cd client && yarn start)\" \"(cd server && yarn start)\"",
    "test": "(cd ../server && yarn test); (cd client && yarn test --no-watch --no-watchAll)",
    "build": "(cd ../server && yarn install && yarn run build); (cd client && yarn install)"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
