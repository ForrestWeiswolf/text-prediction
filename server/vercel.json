{
  "version": 2,
  "github": {
    "enabled": true
  },
  "buildCommand": "echo \"running a build command!\" && yarn run setup",
  "builds": [
    {
      "src": "/index.js",
      "use": "@vercel/node"
    }
  ]
}